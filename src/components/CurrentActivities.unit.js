import CurrentActivities from './CurrentActivities'

describe('@components/CurrentActivities', () => {
  it('exports a valid component', () => {
    expect(CurrentActivities).toBeAComponent()
  })
})
